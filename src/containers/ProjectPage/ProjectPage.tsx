import { Template } from 'layout/Template';
import { FC } from 'react';
import { View } from 'wiloke-react-core';

export const ProjectPage: FC = () => {
  return <Template Content={<View>ProjectPage</View>} />;
};
